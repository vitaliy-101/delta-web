spring:
  application:
    name: delta-web-facade
  datasource:
    url: jdbc:postgresql://127.0.0.1:5433/delta-db?useSSL=false&allowPublicKeyRetrieval=true&createDatabaseIfNotExist=true
    driver-class-name: org.postgresql.Driver
    username: postgres
    password: 007787898
  jpa:
    database-platform: org.hibernate.dialect.PostgreSQLDialect
    database: postgresql
    hibernate:
      ddl-auto: validate
  flyway:
    enabled: true
    locations: classpath:db/migration
  servlet:
    multipart:
      max-file-size: -1
      max-request-size: -1
  kafka:
    bootstrap-servers: localhost:9092
    producer:
      max-request-size: 22000000
      buffer-memory: 22000000
      batch-size: 22000000
  data:
    redis:
      host: localhost
      port: 6380
server:
  port: 8081

